<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tiket Antrean - Indibiz Queue</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Material Icons -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <!-- Alpine.js untuk Animasi & Redirect -->
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
</head>
<body class="bg-[#F8F9FA] min-h-screen font-sans text-[#181C20] selection:bg-[#EE2E24] selection:text-white">

    <!-- Header -->
    <header class="bg-white border-b border-[#E0E3E8] px-4 sm:px-8 py-3.5 flex justify-between items-center sticky top-0 z-40 shadow-sm">
        <div class="flex items-center gap-3">
            <div class="w-10 h-10 rounded-xl bg-[#EE2E24]/10 flex items-center justify-center text-[#EE2E24]">
                <span class="material-symbols-outlined text-[28px]">grid_view</span>
            </div>
            <div>
                <h1 class="text-xl sm:text-2xl font-extrabold text-[#EE2E24] tracking-tight">Indibiz Queue</h1>
            </div>
        </div>
        <div class="flex items-center gap-2 text-xs font-bold text-[#00509E] bg-[#00509E]/10 px-3 py-1.5 rounded-full border border-[#00509E]/20">
            <span class="material-symbols-outlined text-sm">confirmation_number</span>
            <span class="hidden sm:inline">Layar Tiket Digital</span>
        </div>
    </header>

    <!-- Area Konten Utama -->
    <div id="area-tiket-realtime" class="min-h-[calc(100vh-120px)] flex flex-col justify-center items-center p-4 sm:p-8 max-w-5xl mx-auto w-full">
      
      @if($tiket->status == 'Selesai')
          <!-- TAMPILAN KETIKA TIKET SELESAI (DESAIN MINIMALIS TANPA ANGKA COUNTDOWN) -->
          <div x-data="{ progress: 100 }" 
               x-init="
                   let timer = setTimeout(() => {
                       window.location.href = '{{ route('antrean.index') }}';
                   }, 4000);
                   let interval = setInterval(() => {
                       progress -= 25;
                       if (progress <= 0) clearInterval(interval);
                   }, 1000);
               "
               class="bg-white rounded-3xl shadow-2xl border border-[#E0E3E8] p-8 sm:p-10 max-w-lg w-full text-center flex flex-col items-center relative overflow-hidden transform transition-all duration-500">
              
              <!-- Progress Bar Loading Otomatis di Bagian Atas Kartu -->
              <div class="absolute top-0 left-0 w-full h-2 bg-[#F1F4F9]">
                  <div class="h-full bg-emerald-500 transition-all duration-1000 ease-linear" :style="`width: ${progress}%`"></div>
              </div>

              <!-- Animasi Pop-Up & Ping Ring Ikon Centang -->
              <div class="relative mb-6">
                  <div class="w-24 h-24 bg-emerald-100 text-emerald-600 rounded-full flex items-center justify-center border-4 border-emerald-50 shadow-lg animate-[bounce_1s_ease-in-out_1]">
                      <span class="material-symbols-outlined text-6xl">check_circle</span>
                  </div>
                  <span class="absolute inset-0 rounded-full bg-emerald-400/20 animate-ping -z-10"></span>
              </div>

              <h2 class="text-3xl font-black text-[#181C20] mb-2 tracking-tight">Layanan Selesai</h2>
              <p class="text-[#5D3F3B] text-sm sm:text-base mb-6 leading-relaxed">
                  Terima kasih <strong class="text-[#181C20] font-black">{{ $tiket->pelanggan->nama }}</strong>, keperluan Anda telah selesai ditangani oleh petugas kami.
              </p>
              
              <!-- Indikator Loading Bersih & Rata Tengah -->
              <div class="w-full bg-[#F8F9FA] rounded-2xl p-4 border border-[#E0E3E8] flex items-center justify-center gap-2 text-xs font-bold text-[#5D3F3B]">
                  <span class="material-symbols-outlined text-base text-[#00509E] animate-spin">sync</span>
                  <span>Mengarahkan kembali ke layar awal...</span>
              </div>
          </div>

      @elseif($tiket->status == 'Batal')
          <!-- TAMPILAN KETIKA TIKET DIBATALKAN KARENA 2X TIDAK HADIR -->
          <div class="bg-white rounded-3xl shadow-2xl border border-rose-200 p-8 sm:p-10 max-w-lg w-full text-center flex flex-col items-center relative overflow-hidden">
              <div class="absolute top-0 left-0 w-full h-2 bg-[#EE2E24]"></div>

              <div class="w-24 h-24 bg-rose-100 text-[#EE2E24] rounded-full flex items-center justify-center mb-6 border-4 border-rose-50 shadow-md">
                  <span class="material-symbols-outlined text-6xl">cancel</span>
              </div>

              <span class="px-3 py-1 bg-rose-100 text-[#EE2E24] text-xs font-black rounded-full border border-rose-200 uppercase tracking-wider mb-3">
                  Status: Dibatalkan ⚠️
              </span>

              <h2 class="text-3xl font-black text-[#181C20] mb-2 tracking-tight">Tiket Antrean Dibatalkan</h2>
              <p class="text-[#5D3F3B] text-sm sm:text-base mb-6 leading-relaxed">
                  Mohon maaf <strong class="text-[#181C20] font-black">{{ $tiket->pelanggan->nama }}</strong>, nomor antrean <strong class="text-[#EE2E24] font-black">{{ $tiket->nomor_antrian }}</strong> Anda telah dibatalkan otomatis oleh sistem karena tidak hadir di loket CS setelah 2 kali panggilan.
              </p>

              <div class="w-full bg-rose-50/50 p-4 rounded-2xl border border-rose-100 mb-6 text-xs text-[#5D3F3B] font-medium text-left flex items-start gap-3">
                  <span class="material-symbols-outlined text-rose-500 text-lg shrink-0 mt-0.5">info</span>
                  <span>Silakan mengambil tiket antrean baru jika Anda masih membutuhkan layanan konsultasi hari ini.</span>
              </div>

              <a href="{{ route('antrean.index') }}" class="w-full bg-[#EE2E24] hover:bg-[#CE1111] text-white font-black py-4 px-6 rounded-xl transition-all shadow-md flex items-center justify-center gap-2 cursor-pointer">
                  <span class="material-symbols-outlined">add_card</span>
                  <span>Ambil Tiket Baru</span>
              </a>
          </div>

      @else
          <!-- TAMPILAN NORMAL (MENUNGGU / DIPROSES) -->
          <div class="w-full">
              <div class="flex flex-wrap items-center justify-between gap-4 mb-6">
                <a href="{{ route('antrean.index') }}" class="inline-flex items-center gap-2 text-[#5D3F3B] hover:text-[#EE2E24] transition-colors font-semibold text-sm cursor-pointer">
                  <span class="material-symbols-outlined text-lg">arrow_back</span>
                  <span>Kembali ke Pendaftaran Baru</span>
                </a>
        
                <span class="text-xs bg-emerald-50 text-emerald-700 font-bold px-3 py-1 rounded-full border border-emerald-200 flex items-center gap-1.5">
                  <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span>
                  Real-time Sync
                </span>
              </div>
        
              <div class="grid grid-cols-1 xl:grid-cols-12 gap-6">
                <!-- Kolom Kiri: Tampilan Tiket Utama -->
                <div class="xl:col-span-7 flex flex-col gap-6">
                  <div class="bg-white rounded-2xl shadow-md border border-[#E0E3E8] p-8 flex flex-col items-center justify-center text-center relative overflow-hidden">
                    <div class="absolute top-0 left-0 w-full h-2 bg-[#00509E]"></div>
        
                    <span class="px-4 py-1.5 rounded-full text-sm font-extrabold mb-4 border 
                        @if($tiket->status == 'Menunggu') bg-[#78A8FE]/20 text-[#003C7E] border-[#78A8FE]/30
                        @elseif($tiket->status == 'Diproses' || $tiket->status == 'Dipanggil') bg-emerald-100 text-emerald-700 border-emerald-300 animate-pulse
                        @endif">
                      
                      @if($tiket->status == 'Menunggu') ⏳ Sedang Menunggu
                      @elseif($tiket->status == 'Diproses' || $tiket->status == 'Dipanggil') ✅ Silakan Menuju Meja CS
                      @endif
                    </span>
        
                    <h3 class="text-base font-semibold text-[#5D3F3B] mb-1">Nomor Antrean Anda</h3>
        
                    <div class="text-7xl sm:text-8xl font-black text-[#EE2E24] tracking-tight my-2 drop-shadow-sm">
                      {{ $tiket->nomor_antrian }}
                    </div>
        
                    <p class="text-sm sm:text-base text-[#5D3F3B] max-w-md mt-4 font-medium">
                      @if($tiket->status == 'Menunggu')
                          Harap tunggu di area yang disediakan. Layar ini akan diperbarui otomatis saat giliran Anda tiba.
                      @else
                          Petugas kami siap melayani Anda. Silakan menuju ke loket CS.
                      @endif
                    </p>
                  </div>
                </div>
        
                <!-- Kolom Kanan: Detail Antrean -->
                <div class="xl:col-span-5 flex flex-col gap-6">
                  <div class="bg-[#FFFFFF] rounded-2xl shadow-md border border-[#E0E3E8] p-6 flex-1">
                    <h4 class="text-lg font-extrabold text-[#181C20] mb-4 pb-3 border-b border-[#E0E3E8]">Detail Antrean</h4>
        
                    <dl class="space-y-4 text-sm">
                      <div>
                        <dt class="text-xs uppercase text-[#5D3F3B] font-bold tracking-wider">Nama Pelanggan</dt>
                        <dd class="text-base font-bold text-[#181C20] mt-0.5">{{ $tiket->pelanggan->nama }}</dd>
                      </div>
        
                      <div>
                        <dt class="text-xs uppercase text-[#5D3F3B] font-bold tracking-wider">Layanan Dituju</dt>
                        <dd class="text-base font-bold text-[#181C20] mt-0.5 flex items-center gap-2">
                          <span class="material-symbols-outlined text-[#00509E] text-xl">build</span>
                          {{ $tiket->layanan->nama_layanan }}
                        </dd>
                      </div>
        
                      <div>
                        <dt class="text-xs uppercase text-[#5D3F3B] font-bold tracking-wider">Waktu Pengambilan</dt>
                        <dd class="text-sm font-semibold text-[#181C20] mt-0.5">
                            {{ $tiket->created_at ? $tiket->created_at->format('d M Y, H:i') : ($tiket->waktu_dibuat ? $tiket->waktu_dibuat->format('d M Y, H:i') : '-') }}
                        </dd>
                      </div>
        
                      <div>
                        <dt class="text-xs uppercase text-[#5D3F3B] font-bold tracking-wider">Catatan / Keluhan Awal</dt>
                        <dd class="text-sm text-[#181C20] bg-[#F8F9FA] p-3 rounded-xl border border-[#E0E3E8] mt-1.5 italic font-medium">
                          "{{ $tiket->keluhan_awal }}"
                        </dd>
                      </div>
                    </dl>
                  </div>
                </div>
              </div>
          </div>
      @endif

    </div>

    <!-- Script AJAX Polling (Hanya Aktif Jika Belum Selesai & Belum Batal) -->
    @if($tiket->status != 'Selesai' && $tiket->status != 'Batal')
    <script>
        setInterval(function() {
            fetch(window.location.href)
                .then(response => response.text())
                .then(html => {
                    let parser = new DOMParser();
                    let doc = parser.parseFromString(html, 'text/html');
                    
                    let elemenBaru = doc.getElementById('area-tiket-realtime');
                    let elemenLama = document.getElementById('area-tiket-realtime');
                    
                    if (elemenBaru && elemenLama) {
                        elemenLama.innerHTML = elemenBaru.innerHTML;
                    }
                })
                .catch(error => console.error('Gagal memperbarui status tiket:', error));
        }, 3000);
    </script>
    @endif
</body>
</html>